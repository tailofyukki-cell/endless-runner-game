# Game Plan: Endless Runner Expanded

## リスクタスク

### 1. 公平な障害物生成
- **なぜ分離するか:** 障害物の種類・速度・危険度上昇を同時に導入すると、回避不能な連続配置や空中収集物と障害物の重なりが発生しやすい。
- **方針:** 直近に生成した障害物の右端を記録し、ゲーム速度と障害物種別に応じた最小生成距離を満たす場合のみ次の障害物を作る。飛行障害物、地上障害物、コイン、パワーアップは専用の高さ帯を持たせる。
- **検証:** ノーマルとハードをそれぞれ連続プレイし、障害物同士が重ならず、ジャンプ一回で越えられる地上障害物の直後に回避不能な飛行障害物が出現しないことを確認する。

### 2. 状態保存と日替わりチャレンジ
- **なぜ分離するか:** LocalStorageの壊れた値や日付境界がスコア保存・解除状態・日替わり進捗を巻き戻す可能性がある。
- **方針:** 保存形式にバージョン番号と既定値を持たせ、読み込み時に各項目を正規化する。日替わり目標はローカル日付文字列から決定的に選択し、同日中は同一目標を表示する。
- **検証:** ベストスコア、コイン、設定、外観を保存後に再読み込みし、値が維持されることを確認する。日替わり進捗が再読み込み後も継続し、日付キーの変更時だけ目標が更新されることを確認する。

### 3. ポーズとゲームループの復帰
- **なぜ分離するか:** requestAnimationFrameを複数起動すると、再開後にゲーム速度やスコアが二重に進む可能性がある。
- **方針:** ループ開始を単一の識別子で管理し、ゲーム状態が`playing`の場合のみ更新する。ポーズ時には更新を停止し、再開時はカウントダウン完了後に同じループを再開する。
- **検証:** ポーズ・再開を繰り返しても、スコア増加、障害物移動、効果時間が二重化しないことを確認する。

## メインビルド

Canvasアプリを一つの`main.js`から、設定、保存、描画、ゲームワールド、UI制御、入力・音声を扱う小さなモジュール群へ分割する。既存のMVPルールは保持し、コイン、二種のパワーアップ、複数障害物、危険度、難易度別記録、実績、日替わりチャレンジ、テーマ、スキン、ポーズ、設定を加える。

- **アセット:** 生成済みのプレイ画面参照画像を視覚基準に使い、実際のゲーム内表現は外部読込不要のCanvas図形として描画する。
- **検証:**
  - クリック、タップ、Enter、Spaceでゲーム開始・ジャンプ・再開ができる。
  - プレイヤーのジャンプ、着地、障害物との衝突、シールドによる接触無効化が自然に連続する。
  - コイン取得で画面表示・累計値・解除条件が更新される。
  - タイトル画面、ゲーム画面、ポーズ画面、ゲームオーバー画面で文字や操作要素が重ならない。
  - パララックス、昼夜変化、粒子、画面揺れが過剰にならず、動き低減設定で抑制される。
  - 現行ブラウザのコンソールエラーがない。
  - 参照画像との一貫性として、明るい配色、左寄りのランナー、右から迫る障害物、多層背景、読みやすいHUDを満たす。
  - GitHub Pagesの公開URLでタイトル、プレイ、ゲームオーバー、保存データの保持を確認する。
