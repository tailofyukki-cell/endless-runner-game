# Endless Runner Expanded — Structure

## 設計方針

静的なGitHub Pages互換を守り、外部ライブラリを使わないCanvasアプリとして構成する。UIのDOM更新とゲーム規則・描画を分離し、入力イベントからは意味的なアクションだけを発火する。ゲームの状態はタイトル、プレイ中、ポーズ、カウントダウン、ゲームオーバーを明示的に持つ。

## ファイル構成

| パス | 責務 |
|---|---|
| `index.html` | 画面骨格、モーダル、設定・実績・コレクションの操作要素 |
| `styles.css` | レイアウト、配色、フォーカス表示、モバイル最適化、動き低減時の装飾 |
| `main.js` | 起動、画面遷移、入力接続、ゲームループの統合 |
| `js/config.js` | 難易度、テーマ、スキン、実績、日替わり目標、数値定義 |
| `js/storage.js` | LocalStorageの読み書き、保存値の正規化、初期値 |
| `js/audio.js` | Web Audio APIの短い効果音、ミュート状態の管理 |
| `js/renderer.js` | Canvas背景、プレイヤー、障害物、収集物、粒子、HUDの描画 |
| `js/game.js` | ゲーム世界、エンティティ生成、更新、当たり判定、危険度、スコア |
| `EXPANSION_SPEC.md` | 拡張版の仕様と受入条件 |
| `PLAN.md` | リスクと検証条件 |
| `ASSETS.md` | 視覚参照の記録 |

## 実行時の責務

`GameWorld`はプレイヤー、障害物、収集物、パワーアップ、粒子、経過時間を所有する。`Renderer`は`GameWorld`の現在値を読むだけで、ルールを変更しない。`Storage`は永続データを返すだけで、ゲームループに依存しない。`AudioManager`はユーザーの最初の入力後に初期化し、音量とミュート設定に従う。`main.js`はセマンティックな`primaryAction`、`pause`、`openCollection`などをUIイベントに結びつける。

## 状態遷移

| 現在状態 | 操作 | 次状態 | 処理 |
|---|---|---|---|
| タイトル | 難易度開始 | プレイ中 | ワールド初期化、音声解放、初期猶予を開始 |
| プレイ中 | 主操作 | プレイ中 | 地上にいる場合のみジャンプ |
| プレイ中 | ポーズ操作 | ポーズ | 更新停止、ポーズUI表示 |
| ポーズ | 再開 | カウントダウン | 3秒の視覚的カウントダウン |
| カウントダウン | 完了 | プレイ中 | 更新を再開 |
| プレイ中 | 接触 | ゲームオーバー | シールドがあれば消費してプレイ継続、なければ結果保存 |
| ゲームオーバー | リトライ | プレイ中 | 同じ難易度・外観で再初期化 |
| ゲームオーバー | タイトルへ戻る | タイトル | タイトル情報を再描画 |

## 保存データ

保存キーは`endless-runner-expanded-v1`とする。データには難易度別ベスト、累計コイン、解除済み外観、選択中外観、実績、日替わりチャレンジ、音量、ミュート、動き低減設定を含める。無効な保存値や古い保存値を読み込んだ場合は安全な既定値へ正規化する。
